pragma solidity ^0.5.0;
/*
import "./ERC721Full.sol";

contract Land is ERC721Full {
    enum LandType { NFT, PARK, ROAD }

    struct Land {
        address owner;
        LandType landType;
        uint price;
        string content;
    }

    constructor() ERC721Full("Land", "NFT") public {
    }

    function mint() public {
        _mint(msg.sender, 0);
    }
}
*/